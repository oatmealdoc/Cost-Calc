import React from 'react';
import ReactDOM from 'react-dom/client';
import TruCoreCalculator from './components/TruCoreCalculator';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TruCoreCalculator />
  </React.StrictMode>
);